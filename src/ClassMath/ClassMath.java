package ClassMath;

public class ClassMath {
    public static void main(String[] args) {

        double base = 5;
        double expo = 3;

        int result = (int) Math.pow(base, expo);

        System.out.println("El resultado de" + base + "elevado a" + expo + "es" + result);

    }
}
